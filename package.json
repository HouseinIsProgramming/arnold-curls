{
  "name": "arnold-curls",
  "version": "1.0.0",
  "description": "Claude-first GraphQL API flow runner - execute API calls step-by-step with context passing",
  "type": "module",
  "scripts": {
    "run": "bun src/run.ts",
    "status": "bun src/status.ts",
    "test": "bun test",
    "build": "mkdir -p bin && bun build src/run.ts --compile --outfile bin/run && bun build src/status.ts --compile --outfile bin/status"
  },
  "bin": {
    "arnold-curls-run": "./bin/run",
    "arnold-curls-status": "./bin/status"
  },
  "keywords": [
    "graphql",
    "api",
    "flow",
    "claude",
    "ai",
    "testing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
