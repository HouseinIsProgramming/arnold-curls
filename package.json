{
  "name": "arnold-curls",
  "version": "2.0.0",
  "description": "GraphQL API flow runner with multi-set support - execute flows step-by-step with context passing",
  "type": "module",
  "scripts": {
    "arnold": "bun src/arnold.ts",
    "test": "bun test",
    "build": "mkdir -p bin && bun build src/arnold.ts --compile --outfile bin/arnold"
  },
  "bin": {
    "arnold": "./bin/arnold"
  },
  "keywords": [
    "graphql",
    "api",
    "flow",
    "testing"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
